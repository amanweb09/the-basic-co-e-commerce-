<div class="max-w-full overflow-hidden h-20 bg-white flex items-center justify-between px-2">

    <div 
    onclick="toggleMenu()"
    class="ham w-16 h-16 flex-center flex-col cursor-pointer">
        <div class="bars w-10 bg-black rounded-full mb-2"></div>
        <div class="bars w-10 bg-black rounded-full"></div>
    </div>

    <div class="logo">
        <h1 class="text-xl font-extrabold">EESHWEB</h1>
    </div>

    <div class="cart px-2 relative">
        <div class="w-6 h-6 rounded-full bg-orange-500 absolute left-4 bottom-6 flex-center">
            <span class="font-bold text-white">0</span>
        </div>
        <img 
        class="w-6 h-8"
        src="/images/cart.png" 
        alt="cart">
    </div>

    <!-- <div
    style="z-index: 999;" 
    class="sideMenu w-screen h-screen bg-black absolute top-0 hidden"></div> -->

    <script>
        function toggleMenu() {
            const bars = document.querySelectorAll('div.bars')

            bars.forEach((bar) => {
                bar.classList.toggle('open')
            })
        }
    </script>
</div>