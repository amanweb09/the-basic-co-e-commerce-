<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head.ejs') %>
        <title>Products - The Basic Co.</title>
</head>

<body>
    <div class="container mx-auto">

        <%- include('../partials/navbar.ejs') %>

            <div class="grid sm:grid-cols-5 grid-cols-2 gap-6 px-2">
                
                <% products.length && products.map((product)=> { %>

                    <div class="flex-center flex-col w-full shadow-xl px-2">
                        <img class="w-full" src="/images/model.jpg" alt="product">
                        <div class="flex-1">
                            <p class="capitalize"><%= product.title %></p>
                            <p class="text-center font-bold">&#8377;<%= product.price %></p>
                            <button
                            onclick="redirect('/product/<%= product._id %>')" 
                            class="bg-orange-400 w-32 font-bold p-2 my-2 text-sm">View Product</button>
                        </div>
                    </div>
                        <% }) %>

            </div>

            <script>
                function redirect(dest) {
                    window.location.href=dest
                }
            </script>
    </div>
</body>

</html>